name: pre-commit

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: "Build the virtual environment"
        uses: cvxgrp/.github/actions/environment@v2.2.1

      - uses: tschm/cradle/actions/coverage@v0.1.55
        with:
          source-folder: 'tinycta'
          tests-folder: 'tests'
          coveralls: 'true'

  pre-commit:
    runs-on: ubuntu-latest
    steps:
      - uses: tschm/cradle/actions/pre-commit@v0.1.55

  deptry:
    runs-on: ubuntu-latest
    steps:
      - name: "Build the virtual environment"
        uses: cvxgrp/.github/actions/environment@v2.2.2

      - uses: cvxgrp/.github/actions/deptry@v2.2.2
        with:
          source-folder: 'tinycta'
